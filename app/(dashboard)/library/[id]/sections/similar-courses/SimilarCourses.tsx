export type SimilarCoursesProps = {
  course_id: string;
  similarCourses: any;
};

export const SimilarCourses = ({
  course_id,
  similarCourses,
}: SimilarCoursesProps) => {
  return <div></div>;
};
